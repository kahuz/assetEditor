## 파이썬 구현 규칙
- PEP 8 룰을 따라야 함
- 주석 및 설명, 로그는 가능한 한국어 기반으로 작성해야 함
  - 단, 특정 에러 등의 표시를 위해서는 영어를 허용
- 코드 구현 시 클래스 기반으로 SRP 원칙에 맞게 구현해야 함
- 코드 파일 이름은 PEP 8 모듈 명명 규칙에 따라 소문자와 밑줄을 사용해야 함
  - 예: `AssetEditorApplication` 클래스의 파일명은 `asset_editor_application.py`
- 파일 및 문자열은 모두 UTF-8로 처리함